========================================
PMS deployment — quick reference
========================================

Repo project folder:
  d:\Dotnet\CoreCursor\PMSCoditium\PMS

----------------------------------------
A) GCP Ubuntu (production — preferred)
----------------------------------------
Site:    https://pms.coditium.com
Host:    34.131.132.158
SSH:     coditiums@34.131.132.158
Key:     D:\.ssh\Learning\gcp_coditium_vm
App dir: /var/www/pms
Service: pms.service

From PowerShell:
  cd d:\Dotnet\CoreCursor\PMSCoditium\PMS
  powershell -ExecutionPolicy Bypass -File .\deploy-gcp.ps1

Optional overrides:
  .\deploy-gcp.ps1 -SshUser coditiums -IdentityFile "D:\.ssh\Learning\gcp_coditium_vm"

Scripts:
  deploy-gcp.ps1         — publish, upload, run remote script
  deploy-gcp-remote.sh   — unpack + systemd/nginx on the VM

Do NOT deploy to /var/www/pms/app (stale path). Live unit uses /var/www/pms.

----------------------------------------
B) Local / on-prem IIS (Windows)
----------------------------------------
Publish scripts (still valid for Windows IIS targets):
  DeployToIIS.ps1
  DeployToIIS.bat
  DeployToIIS_Unlocked.bat   — when obj\Release is file-locked

Default publish folder is often D:\PMSDeploy or C:\PMSDeploy — confirm IIS site path.

----------------------------------------
C) Other hosts (separate products / VPS)
----------------------------------------
  deploy-hostinger.ps1
  deploy-dlpestate.ps1

These are not the Coditium GCP PMS production deploy.
